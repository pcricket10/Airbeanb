# No need for DATABASE_URL to be set if developing from within a devcontainer

FLASK_APP=app
FLASK_ENV=development
SECRET_KEY=<secure password>
DATABASE_URL=postgresql://<database name>@localhost/<app name>

DATABASE_URL=postgresql://airbeanb@localhost/airbeanb_dev
